spring:
  main:
    allow-bean-definition-overriding: true

cloud:
  aws:
    region:
      static: ap-northeast-2
    stack:
      auto: false

sqs:
  orderEventQueue: order-event-test-queue

s3:
  orderEventBucket: order-event-test-bucket